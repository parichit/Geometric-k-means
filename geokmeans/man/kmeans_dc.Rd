% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kmeans-algos.R
\name{kmeans_dc}
\alias{kmeans_dc}
\title{Run a k-means variant by name}
\usage{
kmeans_dc(
  data,
  centers,
  method = c("geokmeans", "lloyd", "elkan", "hamerly", "annulus", "exponion", "ball"),
  ...
)
}
\arguments{
\item{data}{A numeric matrix or data frame with observations in rows and
features in columns. Missing values are not allowed.}

\item{centers}{Either a single positive integer giving the number of clusters
\code{k}, or a numeric matrix of initial cluster centres (one centroid per row,
with \code{ncol(centers) == ncol(data)}).}

\item{method}{The algorithm to use. One of \code{"geokmeans"}, \code{"lloyd"},
\code{"elkan"}, \code{"hamerly"}, \code{"annulus"}, \code{"exponion"}, \code{"ball"}.}

\item{...}{Further arguments passed to the chosen algorithm.}
}
\value{
An object of class \code{"geokmeans"}; see \code{\link[=geo_kmeans]{geo_kmeans()}}.
}
\description{
A thin dispatcher over the individual algorithm functions.
}
\examples{
set.seed(1)
X <- rbind(matrix(rnorm(100, 0), ncol = 2),
           matrix(rnorm(100, 5), ncol = 2))
kmeans_dc(X, centers = 2, method = "elkan")

}
