% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kmeans-algos.R
\name{kmeans_algorithms}
\alias{kmeans_algorithms}
\alias{geo_kmeans}
\alias{lloyd_kmeans}
\alias{elkan_kmeans}
\alias{hamerly_kmeans}
\alias{annulus_kmeans}
\alias{exponion_kmeans}
\alias{ball_kmeans}
\title{k-Means clustering algorithms}
\usage{
geo_kmeans(
  data,
  centers,
  iter_max = 100L,
  threshold = 0.001,
  init = c("random", "sequential"),
  seed = NULL,
  with_labels = TRUE,
  verbose = FALSE,
  drop_empty = TRUE
)

lloyd_kmeans(
  data,
  centers,
  iter_max = 100L,
  threshold = 0.001,
  init = c("random", "sequential"),
  seed = NULL,
  with_labels = TRUE,
  verbose = FALSE,
  drop_empty = TRUE
)

elkan_kmeans(
  data,
  centers,
  iter_max = 100L,
  threshold = 0.001,
  init = c("random", "sequential"),
  seed = NULL,
  with_labels = TRUE,
  verbose = FALSE,
  drop_empty = TRUE
)

hamerly_kmeans(
  data,
  centers,
  iter_max = 100L,
  threshold = 0.001,
  init = c("random", "sequential"),
  seed = NULL,
  with_labels = TRUE,
  verbose = FALSE,
  drop_empty = TRUE
)

annulus_kmeans(
  data,
  centers,
  iter_max = 100L,
  threshold = 0.001,
  init = c("random", "sequential"),
  seed = NULL,
  with_labels = TRUE,
  verbose = FALSE,
  drop_empty = TRUE
)

exponion_kmeans(
  data,
  centers,
  iter_max = 100L,
  threshold = 0.001,
  init = c("random", "sequential"),
  seed = NULL,
  with_labels = TRUE,
  verbose = FALSE,
  drop_empty = TRUE
)

ball_kmeans(
  data,
  centers,
  iter_max = 100L,
  threshold = 0.001,
  init = c("random", "sequential"),
  seed = NULL,
  with_labels = TRUE,
  verbose = FALSE,
  drop_empty = TRUE
)
}
\arguments{
\item{data}{A numeric matrix or data frame with observations in rows and
features in columns. Missing values are not allowed.}

\item{centers}{Either a single positive integer giving the number of clusters
\code{k}, or a numeric matrix of initial cluster centres (one centroid per row,
with \code{ncol(centers) == ncol(data)}).}

\item{iter_max}{Maximum number of iterations.}

\item{threshold}{Convergence threshold on centroid movement.}

\item{init}{Initialisation strategy when \code{centers} is a number: \code{"random"}
(random observations) or \code{"sequential"} (the first \code{k} observations).
Ignored when \code{centers} is a matrix.}

\item{seed}{Optional integer seed for the random initialisation, or \code{NULL}
(the default). Initialisation uses R's random number generator: supplying a
seed sets it via \code{\link[=set.seed]{set.seed()}} so the result is reproducible, while \code{NULL}
leaves the RNG untouched, so the ambient stream (e.g. a preceding
\code{\link[=set.seed]{set.seed()}} in your session) is honoured.}

\item{with_labels}{Logical; if \code{TRUE} (default) the result includes a
per-observation cluster assignment computed from the final centroids.}

\item{verbose}{Logical; if \code{TRUE}, print the algorithm's convergence message.}

\item{drop_empty}{Logical; if \code{TRUE} (default), clusters that end up with no
assigned observations are removed from the result and the remaining cluster
labels are renumbered, with a message. Requesting more clusters than the
number of distinct rows in \code{data} is always an error.}
}
\value{
An object of class \code{"geokmeans"}: a list with components
\describe{
\item{centroids}{A \verb{k x ncol(data)} matrix of final cluster centres.}
\item{cluster}{Integer vector of cluster ids (1-based), if
\code{with_labels = TRUE}.}
\item{iterations}{Number of iterations performed.}
\item{distance_calculations}{Total number of point-to-centroid distance
computations.}
\item{method}{The algorithm used.}
\item{k}{The number of clusters.}
}
}
\description{
Run one of the bundled k-means variants on a numeric data matrix. All
functions share the same interface and return value; they differ only in the
acceleration strategy used internally. \code{\link[=geo_kmeans]{geo_kmeans()}} runs the bound-free
Geometric-k-means method.
}
\examples{
set.seed(1)
X <- rbind(matrix(rnorm(100, 0), ncol = 2),
           matrix(rnorm(100, 5), ncol = 2))
fit <- geo_kmeans(X, centers = 2)
fit$centroids
table(fit$cluster)

# Supplying explicit starting centroids:
geo_kmeans(X, centers = X[c(1, 51), ])

}
\references{
Sharma, P., Stanislaw, M., Kurban, H., Kulekci, O., and Dalkilic, M. (2026).
Geometric-k-means: A Bound Free Approach to Fast and Eco-Friendly k-means.
\doi{10.1007/s10994-025-06891-1}
}
